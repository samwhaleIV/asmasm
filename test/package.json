{
  "name": "intermediate-langauge-test-server",
  "version": "1.0.0",
  "description": "An HTTP server for serving files from 'src' and 'test' for use in the browser",
  "main": "test-server.js",
  "scripts": {
    "start": "node test-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mistopportunity",
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.16.4"
  }
}
